# RotateListener

RotateListener is an iOS 12 tweak that allows you to toggle your device's orientation lock by pressing on the statusbar. It is a POS (read: work in progress). Install at your own risk (but worst thing that will probably happen is it won't work). 
